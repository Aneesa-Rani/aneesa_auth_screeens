import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primary = Color(0xFF9C4DFF);
  static const secondary = Color(0xFFFF5FA2);

  static const dark = Color(0xFF17151F);
  static const grey = Color(0xFF77727F);

  static const white = Colors.white;
  static const field = Color(0xFFF7F5FA);
}